services:
  next-js:
    container_name: next-js
    env_file:
      - ./.env
    build:
      context: .
      target: prod
    profiles:
      - prod
    volumes:
      # ponytail: same host dir the backend writes uploads to — served by Next.js
      # as static assets from /public, so the admin UI can <img src="/order_files/..">
      - ../backend/services/main-service/storage/order_files:/app/public/order_files
    labels:
      - "traefik.enable=true"
      - "traefik.http.services.frontend.loadbalancer.server.port=3000"
      - "traefik.http.routers.frontend.rule=${DOMAIN}"
      - "traefik.http.routers.frontend.entrypoints=${ENTRYPOINT}"
      - "traefik.http.routers.frontend.tls=${TLS_ENABLED}"
      - "traefik.http.routers.frontend.tls.certresolver=${CERT_RESOLVER}"
    networks:
      - main-network

networks:
  main-network:
    name: main-network
    external: false
  default: